"use client"

import type { ProjectHeroProps } from "@/types/project-detail"

// Componente en desuso: antes renderizaba una galería de miniaturas duplicada.
// Se mantiene vacío por compatibilidad histórica.
export function ProjectHero(_: ProjectHeroProps) {
  return null
}
